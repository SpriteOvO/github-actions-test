name: Build

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install wine
        run: sudo apt-get install wine
      - name: wine hello
        run: wine cmd /c "echo hello"
