name: Build

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        shell: powershell
    steps:
      - uses: actions/checkout@v6
        with:
          repository: SpriteOvO/actions-test-private
          path: TODO
          ssh-key: ${{ secrets.GIT_CLONE_SSH_KEY }}
      - name: Prepare
        run: TODO/scripts/prepare.sh
      - name: Cache
        uses: actions/cache@v5
        with:
          path: |
            ThirdParty
            Libraries
          key: ${{ runner.OS }}-x64-${{ env.CACHE_KEY }}
      - name: Setup VS Env
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
          sdk: ${{ env.WIN_SDK }}
      - name: Build
        run: TODO/scripts/build-win.ps1
      - name: Upload
        if: always()
        run: |
          TODO/scripts/upload.sh ${{ secrets.UPLOAD_TOKEN }} "#ci $env:COMMIT: ${{ job.status }}"
      - name: Install RustDesk for Debug
        shell: powershell
        run: |
          choco install rustdesk.install
          cd "C:\Program Files\RustDesk\"
          ./rustdesk.exe --install-service
          ./rustdesk.exe --get-id | Out-String
          ./rustdesk.exe --password AWeakpASSword
      - name: Block for remote control
        run: sleep 10000000

